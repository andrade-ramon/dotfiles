alias editalias="vim ~/.zsh_aliases"
alias startmysql="mysql.server start"
alias mysqlstart="startmysql"
alias sqls="startmysql"
alias slqs="sqls"
alias slq="sql"
alias sql="mysql -u root -p ''"
alias www="cd /var/www/ && ls"
alias sublime="subl"

#Github
#alias git='hub'
#alias dev="cd $DEV && ls"
alias gt="cd ~/git && ls"

#Maven
#alias mywtf="mvn clean eclipse:clean eclipse:eclipse -Dwtpversion=2.0"

#Utils
alias ll='ls -l'
alias la='ls -la'
alias killtomcat="pl tomcat | awk '{print \$2}' | xargs kill -9"
alias shutdown="sudo shutdown -P 0"
alias u="cd ../"
alias uu="cd ../../"
alias uuu="cd ../../../"
alias uuuu="cd ../../../../"
alias uuuuu="cd ../../../../../"
alias uuuuuu="cd ../../../../../../"
alias rehash="source ~/.zshrc"
alias reahsh=rehash
alias rehahs=rehash



######### Elo7 Alias ###########
#alias vpn='for IP in $(host elo7-read1.c4hwpsqyzt5q.us-east-1.rds.amazonaws.com | awk '\''{ print $NF }'\''); do sudo route add -host $IP $(ifconfig ppp0 | awk '\''/inet/ { print $4 }'\''); done'
#alias vpn2='for IP in $(host elo7-read2.c4hwpsqyzt5q.us-east-1.rds.amazonaws.com | awk '\''{ print $NF }'\''); do sudo route add -host $IP $(ifconfig ppp0 | awk '\''/inet/ { print $4 }'\''); done'








# --------------------------------------------------------------------------------- #
#Tmux
#alias killmux='tmux kill-window -a && exit'
#alias tm='mux start tab'

#alias hey='s -v pt-br "se liga, cabô"'
#alias hlog='heroku logs -t --app vetkatia'
#alias rtomcat='killtomcat && sleep 3 && starttomcat'
#alias restartchrome="killall chrome; google-chrome-stable"

#alias myssh="ssh -i /etc/amazon/tcckey.pem"
#alias myscp="scp -i /etc/amazon/tcckey.pem"
#alias sshtcc="myssh ec2-user@$EC2IP"
#alias connect-rds='myssh -N -L 3306:rds-instance.cdcjqn1hexrd.us-west-2.rds.amazonaws.com:3306 ec2-user@$EC2IP'
#alias mysql-rds='mysql -u kali -p -h 127.0.0.1'

#alias m="mysql -u root -p"

#alias restartNet="sudo service network-manager restart"

#Elo7
#alias pl="ps aux | grep"
#alias elo7.log="tail -f -n 1000 /var/log/elo7/elo7.log"
#alias marketplace="tmuxinator start marketplace"

# Bundler
#alias b="bundle"
#alias bi="b install --path vendor"
#alias bu="b update"
#alias be="b exec"
#alias binit="bi && b package && echo 'vendor/ruby' >> .gitignore"


#alias sshu="ssh -i ~/dev/keys/production_new.pem -l ubuntu"
#alias sshc="ssh -i ~/dev/keys/production_new.pem -l core"#
#alias sshi="ssh -i ~/dev/keys/integration.pem -l ec2-user"
#alias sshpn="ssh -i ~/dev/keys/production.pem -l ec2-user"
#alias sshnat="ssh -i ~/dev/keys/production_new.pem -l ec2-user"

#V3=~/dev/marketplace/elo7/src/main/webapp/v3
#alias rm.assets="rm -rf $V3/common/css && rm -rf $V3/mobile/css && rm -rf $V3/desktop/css && rm -f $V3/common/js/elo7.js && rm -f $V3/mobile/js/mobile.js && rm -f $V3/desktop/js/desktop.js"
#alias wtf="rm.assets; killtomcat; mvn clean eclipse:clean eclipse:eclipse -Dwtpversion=2.0"



# Allows to override Same-Origin policy
#alias ogc='open -a Google\ Chrome --args --disable-web-security'

# Runs grunt in dev
#alias dev='cd /Users/Developer/dev/marketplace/elo7/grunt && ./node_modules/grunt-cli/bin/grunt --gruntfile=gruntfiles/dev.js --base=.'

#alias run-all-tests="mvn clean test -P run-all-tests"
#alias run-migration="flyway-migration"
#alias curl_api="~/dev/dotfiles/scripts/marketplace/mobile/curl_api.sh"
#alias i18n="~/dev/dotfiles/scripts/marketplace/i18n/elo7locale.sh"

# Init tmux
#mux start personal
